import { ErrorMessage } from './error-message'
import { Field } from './field'
import { Input, TextArea, InputMask } from './inputs'
import { Select } from './select'
import { Label } from './label'

export const Form = {
  Label,
  Input,
  InputMask,
  TextArea,
  Select,
  ErrorMessage,
  Field,
}
